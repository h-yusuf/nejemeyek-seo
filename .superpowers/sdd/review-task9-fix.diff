# Review package: dcbb6f8..9b169e2

## Commits
9b169e2 fix: inline error display for delete failures in master page

## Files changed
 src/pages/admin/master.astro | 10 ++++++++--
 1 file changed, 8 insertions(+), 2 deletions(-)

## Diff
diff --git a/src/pages/admin/master.astro b/src/pages/admin/master.astro
index af7f0fd..0f478bc 100644
--- a/src/pages/admin/master.astro
+++ b/src/pages/admin/master.astro
@@ -44,20 +44,21 @@ const [categories, variants] = await Promise.all([
             type="button"
             class="text-sm text-red-500 hover:text-red-700 delete-cat-btn"
             data-id={cat.id}
             data-name={cat.name}
           >
             Hapus
           </button>
         </div>
       ))}
     </div>
+    <p id="cat-delete-error" class="hidden text-red-600 text-sm mt-1"></p>
 
     <!-- Add category form -->
     <div class="bg-white rounded-xl p-4 shadow-sm">
       <p class="font-medium text-stone-700 mb-3 text-sm">Tambah Kategori</p>
       <div class="flex gap-2">
         <input
           type="text"
           id="new-cat-name"
           placeholder="Peyek Rebon"
           class="flex-1 border border-stone-200 rounded-lg px-3 py-2 text-sm focus:outline-none focus:border-brand-400"
@@ -94,20 +95,21 @@ const [categories, variants] = await Promise.all([
             type="button"
             class="text-sm text-red-500 hover:text-red-700 delete-var-btn"
             data-id={String(v.id)}
             data-label={v.label}
           >
             Hapus
           </button>
         </div>
       ))}
     </div>
+    <p id="var-delete-error" class="hidden text-red-600 text-sm mt-1"></p>
 
     <!-- Add variant form -->
     <div class="bg-white rounded-xl p-4 shadow-sm">
       <p class="font-medium text-stone-700 mb-3 text-sm">Tambah Variant</p>
       <div class="flex gap-2">
         <input
           type="text"
           id="new-var-label"
           placeholder="250 gram"
           class="flex-1 border border-stone-200 rounded-lg px-3 py-2 text-sm focus:outline-none focus:border-brand-400"
@@ -165,32 +167,34 @@ const [categories, variants] = await Promise.all([
     } else {
       const { error } = await res.json() as { error: string };
       errEl.textContent = error || 'Terjadi kesalahan'; errEl.classList.remove('hidden');
     }
   });
 
   // Delete category
   document.querySelectorAll('.delete-cat-btn').forEach(btn => {
     btn.addEventListener('click', async () => {
       const el = btn as HTMLButtonElement;
+      const catDelErr = document.getElementById('cat-delete-error')!;
+      catDelErr.classList.add('hidden'); catDelErr.textContent = '';
       if (!window.confirm(`Hapus kategori "${el.dataset.name}"? Semua produk dalam kategori ini juga akan terhapus.`)) return;
 
       const res = await fetch('/admin/api/categories', {
         method: 'DELETE',
         headers: { 'Content-Type': 'application/json' },
         body: JSON.stringify({ id: el.dataset.id }),
       });
 
       if (res.ok) {
         window.location.reload();
       } else {
-        alert('Gagal menghapus kategori');
+        catDelErr.textContent = 'Gagal menghapus kategori'; catDelErr.classList.remove('hidden');
       }
     });
   });
 
   // Add variant
   document.getElementById('add-var-btn')?.addEventListener('click', async () => {
     const label = (document.getElementById('new-var-label') as HTMLInputElement).value.trim();
     const errEl = document.getElementById('var-error')!;
 
     if (!label) { errEl.textContent = 'Label wajib diisi'; errEl.classList.remove('hidden'); return; }
@@ -207,26 +211,28 @@ const [categories, variants] = await Promise.all([
     } else {
       const { error } = await res.json() as { error: string };
       errEl.textContent = error || 'Terjadi kesalahan'; errEl.classList.remove('hidden');
     }
   });
 
   // Delete variant
   document.querySelectorAll('.delete-var-btn').forEach(btn => {
     btn.addEventListener('click', async () => {
       const el = btn as HTMLButtonElement;
+      const varDelErr = document.getElementById('var-delete-error')!;
+      varDelErr.classList.add('hidden'); varDelErr.textContent = '';
       if (!window.confirm(`Hapus variant "${el.dataset.label}"?`)) return;
 
       const res = await fetch('/admin/api/variants', {
         method: 'DELETE',
         headers: { 'Content-Type': 'application/json' },
         body: JSON.stringify({ id: el.dataset.id }),
       });
 
       if (res.ok) {
         window.location.reload();
       } else {
-        alert('Gagal menghapus variant');
+        varDelErr.textContent = 'Gagal menghapus variant'; varDelErr.classList.remove('hidden');
       }
     });
   });
 </script>
